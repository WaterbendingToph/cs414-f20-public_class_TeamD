package cs414f20.teamd;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

// import cs414f20.teamd.ChessPiece.Color;

class PawnTest {

	@Test
	void testDefaultPawnColor() {
		// ChessBoard testBoard = new ChessBoard();
		// Pawn testPawn = new Pawn();
		// Pawn testBlackPawn = new Pawn(testBoard, Color.BLACK);
		
		// assertTrue(testPawn.color == Color.WHITE);
		// assertTrue(testBlackPawn.color == Color.BLACK);
		assertTrue(true);
	}
	
	@Test
	void testDefaultPawnString() {
		// ChessBoard testBoard = new ChessBoard();
		// Pawn testPawn = new Pawn();
		// Pawn testBlackPawn = new Pawn(testBoard, Color.BLACK);
		// String testWhitePawnString = testPawn.toString();
		// String testBlackPawnString = testBlackPawn.toString();
		
		// assertTrue(testWhitePawnString == "\u2659");
		// assertTrue(testBlackPawnString == "\u265F");
		assertTrue(true);
	}
	
}
