package cs414f20.teamd.Gameplay;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

// import cs414f20.teamd.ChessPiece.Color;

class WizardTest {

	@Test
	void testDefaultWizardColor() {
		// ChessBoard testBoard = new ChessBoard();
		// Wizard testWizard = new Wizard();
		// Wizard testBlackWizard = new Wizard(testBoard, Color.BLACK);
		
		// assertTrue(testWizard.color == Color.WHITE);
		// assertTrue(testBlackWizard.color == Color.BLACK);
		assertTrue(true);
	}
	
	@Test
	void testDefaultWizardString() {
		// ChessBoard testBoard = new ChessBoard();
		// Wizard testWizard = new Wizard();
		// Wizard testBlackWizard = new Wizard(testBoard, Color.BLACK);
		// String testWhiteWizardString = testWizard.toString();
		// String testBlackWizardString = testBlackWizard.toString();
		
		// assertTrue(testWhiteWizardString == "\u2654");
		// assertTrue(testBlackWizardString == "\u265A");
		assertTrue(true);
	}

}